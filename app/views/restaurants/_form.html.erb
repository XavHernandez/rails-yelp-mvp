<%= simple_form_for (restaurant) do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :phone_number %>
  <%= r.input :category %>
  <%= r.submit class: 'btn btn-primary my-3' %>
<% end %>
